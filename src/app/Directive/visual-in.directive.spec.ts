import { VisualInDirective } from './visual-in.directive';

describe('VisualInDirective', () => {
  it('should create an instance', () => {
    const directive = new VisualInDirective();
    expect(directive).toBeTruthy();
  });
});
